package integers

// Adding comments above functions, make them appear in GoDoc

// Add takes two integers and returns their sum
func Add(x, y int) int { 
    // same as Add(x int, y int)
    return x + y
}
